<UserControl x:Class="WalletsWPF.Wallets.UserInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WalletsWPF.Wallets"
             mc:Ignorable="d" 
             d:MinHeight="100" d:MinWidth="500" FontSize="15">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding FirstName}"></TextBlock>
        <TextBlock Text="{Binding LastName}" Grid.Column="1"></TextBlock>
        <TextBlock Text="{Binding Login}" Grid.Row="1" Grid.Column="0"></TextBlock>
        <TextBlock Text="{Binding Email}" Grid.Row="1" Grid.Column="1"></TextBlock>
    </Grid>
</UserControl>
